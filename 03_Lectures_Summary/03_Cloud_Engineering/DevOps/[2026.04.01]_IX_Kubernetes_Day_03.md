# Kubernetes Day 03

[1. Services](#1-services)

[2. ClusterIP 실습](#2-clusterip-실습)

[3. Ingress 실습](#3-ingress-실습)

[4. 미션](#4-미션)

[5. Cluster 시스템 구축](#5-cluster-시스템-구축)

## 1. Services

* ClusterIP: 클러스터 내부에서만 접근 가능한 서비스

* NodePort: 클러스터 외부에서 접근 가능한 서비스 (노드의 특정 포트를 통해 접근)

* LoadBalancer: 클러스터 외부에서 접근 가능한 서비스 (로드 밸런서를 통해 접근)

* ExternalName: DNS 이름을 사용하여 외부 서비스를 참조하는 서비스

## 2. ClusterIP 실습

### 2.1 nginx-clusterip.yaml

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-pod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: registry.k8s.io/nginx:latest
        ports:
        - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 80   
  type: ClusterIP
```

### 2.2 Deployment & Service 생성

```bash
# Deployment & Service 생성
kubectl apply -f nginx-clusterip.yaml

# Deployment & Service 확인
kubectl get deployments
kubectl get services

# Pod 확인
kubectl get pods
```

### 2.3 Pod 내부로 접근

```bash
# Pod 내부로 접근
kubectl exec -it <pod-name> -- /bin/sh

# Curl 명령어로 서비스에 접근
curl 10.244.0.49
curl 10.244.0.50
```

## 3. Ingress 실습

### 3.1 Ingress 설치

```bash
# NGINX Ingress Controller 설치
kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/cloud/deploy.yaml

# Namespace 확인
kubectl get namespaces

# 모든 Nginx 리소스 확인
kubectl get all -n ingress-nginx

# NGINX Ingress Controller 확인
kubectl get pods -n ingress-nginx

# Minikube Ingress 활성화
minikube addons enable ingress
```

### 3.2 실행

* nginx-ingressdeploysvc.yaml

    ```yaml
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: nginx-deployment
    spec:
      replicas: 2
      selector:
        matchLabels:
          app: nginx
      template:
        metadata:
          labels:
            app: nginx
        spec:
          containers:
          - name: nginx
            image: registry.k8s.io/nginx:latest
            ports:
            - containerPort: 80

    ---
    apiVersion: v1
    kind: Service
    metadata:
      name: nginx-service
    spec:
      selector:
        app: nginx
      ports:
        - port: 80
          targetPort: 80
      type: ClusterIP
    ```

* nginx-ingress.yaml

    ```yaml
    apiVersion: networking.k8s.io/v1
    kind: Ingress
    metadata:
      name: nginx-ingress
      namespace: default
      annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /
    spec:
      rules:
      - host: nginx.local
        http:
          paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-service
                port:
                  number: 80
    ```

* YAML 파일 적용

    ```bash
    kubectl apply -f nginx-ingressdeploysvc.yaml
    kubectl apply -f nginx-ingress.yaml
    ```

### 3.3 Ingress 확인

```bash
# Ingress 확인
kubectl get ingress nginx-ingress

# Ingress EXTERNAL-IP 확인
kubectl get services -n ingress-nginx
kubectl get nodes -o wide
```

### 3.4 Ingress 접근

```bash
# Ingress 접근
curl http://192.168.49.2
# 404 Not Found 에러 발생

# /etc/hosts 파일에 Ingress IP와 호스트명 추가
sudo nano /etc/hosts
# 192.168.49.2 nginx.local

curl http://nginx.local
# NGINX 환영 페이지가 표시됨
```

## 4. 미션

* nginx-ingressdeploysvc-fisa.yaml

    ```yaml
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: fisa-nginx-dep
    spec:
      replicas: 3
      selector:
        matchLabels:
          app: fisans
      template:
        metadata:
          labels:
            app: fisans
        spec:
          containers:
          - name: nginx
            image: registry.k8s.io/nginx:latest
            ports:
            - containerPort: 80

    ---
    apiVersion: v1
    kind: Service
    metadata:
      name: fisa-nginx-svc
    spec:
      selector:
        app: fisans
      ports:
        - port: 80
          targetPort: 80
      type: ClusterIP
    ```

* nginx-ingress-fisa.yaml

    ```yaml
    apiVersion: networking.k8s.io/v1
    kind: Ingress
    metadata:
      name: fisa-nginx-ingress
      namespace: default
      annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /
    spec:
      rules:
      - host: fisa.nginx
        http:
          paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fisa-nginx-svc
                port:
                  number: 80
    ```

* /etc/hosts 파일에 Ingress IP와 호스트명 추가

    ```bash
    sudo nano /etc/hosts
    # 192.168.49.2 fisa.nginx
    ```

* YAML 파일 적용

    ```bash
    kubectl apply -f nginx-ingressdeploysvc-fisa.yaml
    kubectl apply -f nginx-ingress-fisa.yaml
    ```

## 5. Cluster 시스템 구축

### 5.1 Node 준비

* Core CPU, 4GB RAM, 30GB Disk

* Hostname 설정: server01, server02, server03

* Network 설정: 10.0.2.15, 10.0.2.20, 10.0.2.25

* 시스템 업데이트: `sudo apt update && sudo apt upgrade -y`

* SSH 활성화: `sudo systemctl enable --now ssh`

* 시간대 설정: `sudo timedatectl set-timezone Asia/Seoul`

* SSH 키 생성 및 배포

    ```bash
    # SSH 키 생성
    ssh-keygen -t rsa -b 4096 -C ""

    # SSH 키 확인
    cat ~/.ssh/id_rsa.pub

    # SSH 키 배포
    ssh-copy-id user@server01
    ssh-copy-id user@server02
    ssh-copy-id user@server03

    # 필요시 권한 설정
    chmod 400 ~/.ssh/id_rsa ~/.ssh/id_rsa.pub
    ```

* /etc/hosts 파일에 노드 IP와 호스트명 추가: `sudo nano /etc/hosts`

    ```ini
    10.0.2.15 server01
    10.0.2.20 server02
    10.0.2.25 server03
    ```

* SWAP 비활성화: `sudo swapoff --all`

    ```bash
    # SWAP 비활성화
    sudo nano /etc/fstab
    # /swap.img none swap sw 0 0

    # SWAP 비활성화 확인
    free -h
    ```

### 5.2 Kubernetes - 공통 설치

#### 1. 필요한 커널 모듈 설정 및 로드

```bash
# 루트 사용자로 전환
sudo -i

# 모듈 로드 디렉토리 확인
tree /etc/modules-load.d/

# 필요한 커널 모듈 설정
cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF

# 커널 모듈 로드
modprobe overlay
modprobe br_netfilter

# 커널 파라미터 설정
cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward = 1
EOF

# 커널 파라미터 적용
sysctl --system
```

#### 2. Kubernetes 패키지 설치

```bash
# Containerd 설치
sudo apt update && sudo apt install -y containerd

# Containerd 설정
sudo mkdir -p /etc/containerd

# Containerd 기본 설정 생성
sudo containerd config default | sudo tee /etc/containerd/config.toml

# Systemd cgroup 설정 true로 변경
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml

# Containerd 재시작
sudo systemctl restart containerd

# 필요시 기존 설정 초기화
sudo rm -f /etc/apt/sources.list.d/kubernetes.list
sudo rm -f /etc/apt/trusted.gpg.d/kubernetes.gpg

# 필요한 패키지 설치
sudo apt update && sudo apt install -y apt-transport-https ca-certificates

# Kubernetes GPG 키 추가
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.30/deb/Release.key | \
    sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg

# Kubernetes APT 저장소 추가
echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] \
    https://pkgs.k8s.io/core:/stable:/v1.30/deb/ /" | \
    sudo tee /etc/apt/sources.list.d/kubernetes.list

# 저장소 활성화 옵션 추가
sudo tee /etc/apt/apt.conf.d/99allow-insecure-repositories <<EOF
APT::Get::AllowUnauthenticated "true";
Acquire::AllowInsecureRepositories "true";
Acquire::AllowDowngradeToInsecureRepositories "true";
EOF

# kubeadm, kubelet, kubectl 설치
sudo apt update --allow-unauthenticated
sudo apt install -y --allow-unauthenticated kubeadm kubelet kubectl

# 설치된 패키지 버전 확인
kubectl version --client --output=yaml
kubeadm version --output=yaml
kubelet --version
```

### 5.3 Kubernetes - Master Node 설정

```bash
# 인증서 만료 시간 확인
sudo kubeadm certs check-expiration

# kubeadm 사용할 수 있는 이미지 리스트 확인
sudo kubeadm config images list

# containerd에서 kubeadm이 사용할 이미지 미리 풀링
sudo kubeadm config images pull --cri-socket unix:///run/containerd/containerd.sock

# kubeadm init 실행
sudo kubeadm init \
    --apiserver-advertise-address=10.0.2.15 \
    --pod-network-cidr=192.168.0.0/16 \
    --cri-socket /run/containerd/containerd.sock

# To start using your cluster, you need to run the following as a regular user:
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config

# CNI Calico 설치
curl -O https://raw.githubusercontent.com/projectcalico/calico/v3.27.3/manifests/calico.yaml
kubectl apply -f calico.yaml --validate=false
```

### 5.4 Kubernetes - Worker Node 설정

```bash
# .kube 폴더 생성
mkdir -p ~/.kube

# .kube/config 파일 복사
scp -p fisa@server01:~/.kube/config ~/.kube/config

# kubeadm join 명령어 실행
sudo kubeadm join 10.0.2.15:6443 \
    --token <TOKEN> \
    --discovery-token-ca-cert-hash <HASH>
```

### 5.5 Cluster 확인

```bash
# 노드 상태 확인
kubectl get nodes -o wide

# Cluster 정보 확인
kubectl cluster-info
```

### 5.6 Cluster Test

```bash
# nginx Deployment 생성
kubectl create deployment nginx1 --image=registry.k8s.io/nginx --replicas=3

# Pod 상태 확인
kubectl get pods -o wide

# Nginx Deployment 삭제
kubectl delete deployment nginx1
```